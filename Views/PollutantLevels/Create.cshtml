@model AQIViewer.Models.PollutantLevel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PollutantLevel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MinConcentration" class="control-label"></label>
                <input asp-for="MinConcentration" class="form-control" />
                <span asp-validation-for="MinConcentration" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MaxConcentration" class="control-label"></label>
                <input asp-for="MaxConcentration" class="form-control" />
                <span asp-validation-for="MaxConcentration" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PollutionLevelId" class="control-label"></label>
                <select asp-for="PollutionLevelId" class ="form-control" asp-items="ViewBag.PollutionLevelId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PollutantId" class="control-label"></label>
                <select asp-for="PollutantId" class ="form-control" asp-items="ViewBag.PollutantId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
