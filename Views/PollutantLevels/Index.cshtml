@model IEnumerable<AQIViewer.Models.PollutantLevel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MinConcentration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxConcentration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PollutionLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pollutant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LevelRecomendation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MinConcentration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxConcentration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PollutionLevel.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pollutant.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LevelRecomendation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
